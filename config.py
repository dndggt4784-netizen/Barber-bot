import os
from dotenv import load_dotenv

load_dotenv()

# Токен бота — получить у @BotFather в Telegram
BOT_TOKEN = os.getenv("BOT_TOKEN", "")

# ID чата владельца (куда приходят уведомления о новых записях).
# Как получить: напишите боту @userinfobot — он покажет ваш chat_id.
ADMIN_CHAT_ID = os.getenv("ADMIN_CHAT_ID", "")

# Название заведения — отображается в приветствии и в уведомлениях
BUSINESS_NAME = os.getenv("BUSINESS_NAME", "Барбершоп Стиль")

# Адрес — отображается в подтверждении записи
BUSINESS_ADDRESS = os.getenv("BUSINESS_ADDRESS", "ул. Ленина, 10")

# Список услуг: (название, цена в гривнах)
SERVICES = [
    ("Стрижка", 500),
    ("Борода", 300),
    ("Стрижка + борода", 700),
]

# Время работы — слоты, которые бот предлагает клиенту на выбор.
# Занятые слоты автоматически скрываются (см. db.py).
TIME_SLOTS = [
    "10:00", "11:00", "12:00", "13:00",
    "14:00", "15:00", "16:00", "17:00", "18:00",
]

# На сколько дней вперёд разрешаем запись
DAYS_AHEAD = 7
